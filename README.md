Verify if `pyenv` is installed
- If not, install it

Verify if `python=3.12.5` is installed
- If not, install it

Verify if `pipx` is installed
- If not, install it

Verify if `poetry` is installed
- If not, install it

>> poetry install

